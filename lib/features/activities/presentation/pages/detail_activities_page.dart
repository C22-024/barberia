import 'package:flutter/material.dart';

class DetailActivitiesPage extends StatelessWidget {
  final int id;

  const DetailActivitiesPage({super.key, required this.id});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
